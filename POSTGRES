CREATE TABLE aggregated_file ( publisher varchar(200), dataset varchar(200), statname varchar(200), data json);
CREATE TABLE aggregated_publisher ( publisher varchar(200), statname varchar(200), data json);
CREATE TABLE aggregated ( publisher varchar(200), statname varchar(200), data json);
